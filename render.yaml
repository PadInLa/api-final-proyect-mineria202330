services:
  - type: web
    name: api-pf-mineriadedatos2023
    env: python
    plan: free # e.g., starter, standard, pro
    buildCommand: pip install -r requirements.txt # e.g., pip install -r requirements.txt
    startCommand: py WebFlasfk.py # e.g., flask run
    pythonVersion: 3.11 # e.g., 3.8
    region: oregon # e.g., oregon, frankfurt
